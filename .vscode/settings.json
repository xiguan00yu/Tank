{
    "saveAndRun": {
        "commands": [
            {
                "match": ".*",
                "isAsync": true,
                "cmd": "scp -r src pi@pi.com:~/Project/Tank/. && scp -r package.json pi@pi.com:~/Project/Tank/. && echo 'SCP Success !'"
            }
        ]
    }
}
